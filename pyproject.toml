[tool.poetry]
name = "zeroops-client"
version = "0.1.0"
description = "ZeroOps Workshop TUI Client"
authors = ["Admin <admin@example.com>"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.10"
textual = "^0.80.0"
typer = "*"
httpx = "^0.26.0"
pydantic-settings = "^2.1.0"
pyyaml = "^6.0.3"
keyring = "^25.0.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.pyright]
venvPath = "."
venv = ".venv"
extraPaths = ["src"]


