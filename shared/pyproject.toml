[project]
name = "zeroops-shared"
version = "0.1.0"
description = "ZeroOps Workshop - Shared Models & Constants"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
